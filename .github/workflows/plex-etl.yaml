name: 'Daily Plex Watch History Sync'

on:
    schedule:
        - cron: '15 0 * * *' # Runs daily, offset from the photo sync
    workflow_dispatch: # Allows you to click "Run Now" manually for testing

concurrency:
    group: daily-plex-sync
    cancel-in-progress: false

jobs:
    update-data:
        runs-on: ubuntu-latest
        permissions:
            contents: write # Crucial! Allows the robot to commit changes

        steps:
            - name: Check out repo
              uses: actions/checkout@v7

            - name: Set up Python
              uses: actions/setup-python@v6
              with:
                  python-version: '3.12'

            - name: Cache pip
              uses: actions/cache@v6
              with:
                  path: ~/.cache/pip
                  key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}
                  restore-keys: |
                      ${{ runner.os }}-pip-

            - name: Install dependencies
              run: |
                  set -euo pipefail
                  python -m pip install --upgrade pip build
                  echo "Installing project and dependencies from pyproject.toml"
                  pip install .

            - name: Run Plex ETL Script
              env:
                  PLEX_URL: ${{ secrets.PLEX_URL }}
                  PLEX_TOKEN: ${{ secrets.PLEX_TOKEN }}
                  TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
              run: |
                  set -euo pipefail
                  python backend/plex_etl.py

            - name: Set up Node
              uses: actions/setup-node@v6

            - name: Format synced content with Prettier
              # PyYAML's quoting choices (e.g. single-quote-escaping an
              # apostrophe) don't always match Prettier's, which the "test"
              # workflow enforces via pre-commit. Format here, before
              # committing, so generated content never fails that check.
              # Version/args must match the prettier hook in
              # .pre-commit-config.yaml.
              run: |
                  npx --yes prettier@4.0.0-alpha.8 --write --tab-width=4 --print-width=100 "content/watch-items/**/*.yaml"

            - name: Commit and Push if changed
              run: |
                  git config user.name "GitHub Action"
                  git config user.email "action@github.com"
                  git add content/watch-items/
                  git diff --quiet && git diff --staged --quiet || (git commit -m "🎬 Update watch items - $(date +'%Y-%m-%d')" && git push)
